// import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { QuizState } from "./quiz.state";

// export const root = createFeatureSelector<QuizState>('quiz');

// export const questions = createSelector(root, state => state.questions);

// export const answers = createSelector(root, state => state.answers);

// export const currentQuestionIndex = createSelector(answers, all => all.length);

// export const currentQuestion = createSelector(questions, currentQuestionIndex, (all, index) => all[index]);